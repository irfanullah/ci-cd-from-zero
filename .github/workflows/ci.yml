name: CI Pipeline

on:
  push:
    branches: ["*"]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run CI checks
        run: bash module-04-failing-ci/check.sh
